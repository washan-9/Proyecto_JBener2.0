/**
 * api.js - Capa de conexión con el Backend (Fase 4)
 * Aquí se centralizarán todas las peticiones fetch, manejo de JWT,
 * y configuración de la API de FastAPI.
 */

const API_BASE_URL = 'http://localhost:8000/api';

const api = {
    // Ejemplo de futuro método
    // async login(email, password) { ... }
};
